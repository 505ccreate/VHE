# VHE — Video Hallucination Editor

Canonical branch: `main`.

This repository is being initialized from the authoritative `VHE-Progress-update 16_2026-07-27.zip` baseline so the project can continue without rebuilding completed work.

Logging is mandatory for every builder and every handoff. See `_LOGS/` once the baseline import is complete.

**Migration lead/signature:** Eli Soren
