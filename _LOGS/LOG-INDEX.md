# Issue & Resolution Log — Index

Lookup table for the diary. **Do not read every entry.** Use this to find the one you need.

Numbering is consecutive across the entire project and never restarts for a new platform, room,
phase, or AI account.

**Before writing a new entry, claim its number by adding its row here first.** This prevents two
rooms working in parallel from colliding on the same number.

| # | Title | Status | Blueprint | Logged by | Date |
|---|---|---|---|---|---|
| [0001](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0001_project-state-audit-no-code-exists_CC-OPUS-01.md) | Project state audit: no code exists, Phase 0 not started | RESOLVED | VHE-2 §1, §16 · VHE-3 | `CC-OPUS-01` | 2026-07-19 |
| [0002](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0002_vhe5-filename-version-mismatch_CC-OPUS-01.md) | VHE-5 filename said v1.0, contents v1.1 — renamed to v1_1 | RESOLVED | VHE-5 header, §B1–§B11 | `CC-OPUS-01` | 2026-07-19 |
| [0003](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0003_unanswered-phase0-blocking-decisions_CC-OPUS-01.md) | VHE-2 §17 decisions — **Q1 stack + Q3 API-only ANSWERED**; Q2/Q4/Q5/Q6 open | PARTIAL | VHE-2 §17 · VHE-1 · VHE-3 · VHE-4 §A12 | `CC-OPUS-01` | 2026-07-19 |
| [0004](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0004_docx-only-blueprints-token-cost_CC-OPUS-01.md) | .docx-only blueprints — `_BLUEPRINTS-TEXT/` mirror + regen script created | RESOLVED | N/A | `CC-OPUS-01` | 2026-07-19 |
| [0005](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0005_hardware-compatibility-audit_CC-OPUS-01.md) | Hardware/env audit: no CUDA GPU; Node 24 vs pin 22; system ffmpeg 8.1.2 hazard; Windows alternatives for source-tarball bundles | PARTIAL | VHE-2 §1 · VHE-3 | `CC-OPUS-01` | 2026-07-19 |
| [0006](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0006_project-tools-library-integrated_CC-OPUS-01.md) | Owner's `library/` (52 verified bundles, API-only profile) audited and wired in | RESOLVED | VHE-2 §1 · VHE-3 | `CC-OPUS-01` | 2026-07-19 |
| [0007](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0007_free-tier-hosting-vs-vhe2-architecture_CC-OPUS-01.md) | Free-tier hosting (Vercel/Firebase/Supabase) vs VHE-2 long-running services — production worker topology open | **DEFERRED** | VHE-2 §0, §2, §4, §14 | `CC-OPUS-01` | 2026-07-19 |
| [0008](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0008_fixture-count-discrepancy-seven-vs-eight_CC-OPUS-01.md) | Handoff said 7 test fixtures; VHE-2 §1 requires **8** (7-17 patch added `bad_hand_6s.mp4`) | RESOLVED | VHE-2 §1 · §9.4 | `CC-OPUS-01` | 2026-07-19 |
| [0009](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0009_fixtures-not-buildable-in-this-room_CC-OPUS-01.md) | 4 of 8 fixtures need AI-generated content (API keys the owner holds); other 4 need FFmpeg recipes §6 does not define | **UNRESOLVED** | VHE-2 §0, §1, §6 | `CC-OPUS-01` | 2026-07-19 |
| [0010](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0010_preflight-scaffold-and-toolchain-pinned_CC-OPUS-01.md) | Phase 0 scaffold built: repo, vendored FFmpeg 7.1.1, Node 22 via fnm, `scripts/preflight.ts` running | PARTIAL | VHE-2 §1 · §16 | `CC-OPUS-01` | 2026-07-19 |
| [0011](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0011_owner-rulings-fixtures-built-hosted-service-route_CC-OPUS-01.md) | Owner rulings + fixtures built + toolchain locked + all 3 hosted services live (S3/Redis PASS, PG connects via IPv4 pooler); **open: PG 17.6 vs 16 pin, 4 AI fixtures** | PARTIAL | VHE-2 §0, §1, §6.3, §16, §17 Q1 | `CC-OPUS-01` | 2026-07-20 |

| [0012](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0012_pg17-ruling-gate-waiver-section2-schema_CC-FABLE-01.md) | Owner rulings: PG 17 accepted, fixture-gate waived; §2 schema APPLIED+verified live; .docx smart-quote corruption in code blocks + de-autocorrect mapping documented | RESOLVED | VHE-2 §0, §2, §16, §17 Q1 | `CC-FABLE-01` | 2026-07-19 |

| [0013](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0013_section6-wrapper-61-goldentest-contradiction_CC-FABLE-01.md) | §6 wrapper built; **§6.1 verbatim code contradicted its own mandatory golden test** — owner-ruled half-ms epsilon fix; both §6 golden tests now PASS; vitest scoped away from library/ | RESOLVED | VHE-2 §0, §3, §6 | `CC-FABLE-01` | 2026-07-20 |

| [0014](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0014_section3-ingest-probe-core-vfr-conform_CC-FABLE-01.md) | §3 ingest/probe correctness core built + tested: rational-fps parse, probe→fields, sha256, VFR detect→§6.7 conform; verified on all 4 structural fixtures (11/11 tests) | RESOLVED | VHE-2 §0, §3, §6.7 | `CC-FABLE-01` | 2026-07-20 |

| [0015](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0015_section4-job-lifecycle-idempotency-live_CC-FABLE-01.md) | §4 job lifecycle: idempotency claim + §4.3 taxonomy + §4.2 transition; **BOTH exit-gate clauses verified LIVE** (1 row/1 enqueue, budget gate, optimistic transition, heartbeat takeover→no second bill); production BullMQ transport is the only remainder | RESOLVED | VHE-2 §4.1, §4.2, §4.3 | `CC-FABLE-01` | 2026-07-20 |

| [0016](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0016_section5-masks-docx-dropped-tokens_CC-FABLE-01.md) | §5 mask format built + IoU exit gate PASSES; **the .docx dropped compile-required tokens from its own §5 code** (generics, template backticks, the opening `<svg>` tag) — each reconstruction listed | RESOLVED | VHE-2 §5 | `CC-FABLE-01` | 2026-07-20 |

| [0017](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0017_section4-bullmq-transport-retry-wiring-defect_CC-OPUS-01.md) | §4 production BullMQ transport built (Queue/Worker wiring `queues[type].add` → `executeClaimed`); **§4.1 verbatim add-options make §4.2's retry branch unreachable** — delegated ruling, owner review requested | RESOLVED (**2 rulings need owner review**) | VHE-2 §4.1, §4.2, §4.3 | `CC-OPUS-01` | 2026-07-20 |

| [0018](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0018_owner-ruling-provider-ranking-unblocks-section7_CC-OPUS-01.md) | **Owner ruling (via Eli/ChatGPT): §17 Q2 answered** — capability-filtered routing, visual order fal.ai→Replicate→Google→OpenAI, audio Google→OpenAI→ElevenLabs; 4 fixtures to arrive as frozen files; keep PG17/waiver/§6.1 + add fps boundary tests. **Unblocks §7.** | RESOLVED (ruling recorded) | VHE-2 §7, §17 Q2 · VHE-4 | `CC-OPUS-01` | 2026-07-20 |

| [0019](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0019_section7-provider-adapters-routing-built_CC-OPUS-01.md) | **§7 built: BYOK provider adapters + capability routing.** `packages/providers/*` — AES-256-GCM key storage, routeChain (default-first, fal→replicate→google→openai fallback), runGeneration/walkChain, cost estimation. **§7 exit gate PASSES live** (2 mock adapters, first fails, second wins, job row shows 2nd provider_id+cost). docx dropped 2 generics (0016 class); config-table-vs-code flagged. §6.1 fps boundary tests added. **32/32.** | RESOLVED | VHE-2 §7 · §6.1 | `CC-OPUS-01` | 2026-07-20 |

| [0020](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0020_section9-1-image-inpaint-core_CC-SONNET-01.md) | **§9.1 image inpaint core built.** `packages/repair/inpaint.ts` — mask bbox+25% margin (clamped), crop-inpaint-paste, verbatim prompt/negative template compile (user-first precedence), candidate compositing via the §9.2 mask rule, `runImageInpaint` orchestration over `runGeneration` (§7) with injectable storage. **§9.1 is prose (no code block, no exit gate — confirmed vs .docx).** Verified: 10/10 new + **42/42 suite** live. §11 "Apply" deferred (edit-graph not built); provider-max downscale edge flagged. | RESOLVED | VHE-2 §9.1 · §5 · §7 | `CC-SONNET-01` | 2026-07-20 |

| [0021](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0021_section9-1-hardening-eli-review_CC-SONNET-01.md) | **§9.1 hardening patch (Eli review, conditional-pass).** All 5 Eli findings verified vs source + blueprint, then fixed in `packages/repair/inpaint.ts`: (1) crop box now contains the mask feather; (2) oversized crop **downscaled to provider max** + composited back at full res (replaces "route around" — blueprint literally says "clamped to provider maxWidth/Height"); (3) zero-candidate result rejected + honest count; (4) input validation; (5) bounded default-negative conflict subtraction; ICC/EXIF metadata preserved (orientation→§3 ingest, flagged). Owner+Eli rulings recorded (§4 retries OK, defer §11, cost-defaults in code, web/single-user-first, temp fixtures ≠ preflight-green). **55/55 suite live** (+13). Real-provider validation still OPEN (needs adapter+key+real fixtures). | RESOLVED (patch) · provider-validation OPEN | VHE-2 §9.1 · §7 · §5 · §3 | `CC-SONNET-01` | 2026-07-21 |

| [0022](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0022_real-provider-inpaint-validation_CC-SONNET-01.md) | **First REAL paid provider inpaint validation (§9.1 end-to-end).** No fal key exists in the library (registry marks fal/replicate reference-only) → owner chose OpenAI (+ Gemini "both"). Built real adapters `packages/providers/adapters/{openai-image,gemini-image}.ts` + `storage-seam.ts`, wired the OpenAI key **encrypted** into `provider_connections` (KEK added to `.env`), created temp synthetic fixtures in a subdir (do NOT green preflight FAIL 4). **OpenAI gpt-image-1 PASSED live** through the real §7 (DB-decrypt) → §9.1 (crop→composite) path: 2 fixtures × 2 candidates, mask-scoped + non-destructive (outside-mask delta 0, verified), ~13¢/fixture est. **Gemini free key rate-limited (429)** — adapter/error-mapping correct, no charge. Plumbing validated, NOT repair quality (synthetic fixtures; real §1 fixtures still owed). Findings: adapter interface has no storage seam; adapters return URLs/b64 not keys; provider cost absent from API (usage-estimate only). **67/67 suite.** | RESOLVED (OpenAI) · Gemini BLOCKED (quota) · quality-validation OPEN | VHE-2 §7 · §9.1 · §2 | `CC-SONNET-01` | 2026-07-21 |

| [0023](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0023_higgsfield-teardown-source-assessment_CC-SONNET-01.md) | **Assessed new source `VHE-ADDON-SOURCE-1_Higgsfield_Clone_Teardown` (Marcus).** Verdict: NOT too late — it targets the UNBUILT generation layer (video.t2v/i2v/extend, §7/§10/§9.3), downstream of the current repair core (§2–§9.1); nothing built conflicts. Most of it validates existing architecture. One new actionable: OpenRouter unified video-gen API (Apr 2026) + live capability-manifest endpoint `/api/v1/videos/models` → a future §7 video connection + live-hydrated manifest (vs the hardcoded manifests in the `0022` adapters). §6 open questions deferred to the generation-layer phase. **No code changed** (not authorized; repair core is priority). Filed as source-library reference. | REFERENCE (deferred to gen-layer) | VHE-2 §7 · §10 · §9.3 · VHE-1 | `CC-SONNET-01` | 2026-07-21 |

| [0024](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0024_section9-2A-deterministic-core-and-provider-refactors_CC-HAIKU-01.md) | **§9.2A deterministic video-repair core + provider-layer refactors (Eli Q1–Q4 rulings).** Owner returned Eli's rulings: build §9.2A deterministic core now (no GPU/no real fixtures → quality still UNVALIDATED); replace the `0022` storage factory-closure with a **versioned `ProviderExecutionContext`** (adapters read buffers / request short-lived signed input URLs, return provider-native bytes/URLs; worker/registry owns download→validate→deterministic-store→assetKey); **versioned per-provider/per-model pricing catalog** (raw usage + estimated + reported + provenance; dashboard authoritative when no exact cost); **hybrid manifest cache seam** (refresh on connect + 24h TTL, last-known-good, manual snapshots). Higgsfield gen-layer stays deferred (OpenRouter-first video, direct adapters later, hosted consent-gated identity before self-hosted face-swap). Built 4 new modules (execution-context, cost-catalog, manifest-cache, video-repair), migrated both real adapters off the closure, deleted storage-seam. **Suite 61→109 GREEN; preflight unchanged 13/4/1.** Repair-QUALITY still OPEN (owner's real-fixture gate). Also corrected the stale "67/67" suite claim → real baseline was 61/61. | **RESOLVED** (build) · quality-validation OPEN | VHE-2 §9.2 · §6.1 · §6.3 · §6.4 · §7 · §5 · §8 · VHE-4 (manifest TTL) | `CC-HAIKU-01` | 2026-07-21 |

| [0025](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0025_section9-5-window-count-contradiction_CODEX-SOL-01.md) | **§9.5 deterministic core built + blueprint contradiction resolved:** W=48/O=8 requires **8**, not 7, children for 300 frames. Full coverage, global keys/seed, deterministic child ids, parent linkage, restart selection, mask-only overlap blend + SSIM, and env memory ceiling implemented. **123/123 green; preflight 13/4/1 unchanged.** Real video/GPU quality exit remains blocked. | PARTIAL (deterministic core GREEN; real-quality exit OPEN) | VHE-2 §9.5 · VHE-5 §B3/B5 | `CODEX-SOL-01` | 2026-07-22 |

| [0026](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0026_fal-image-inpaint-adapter_CC-OPUS-01.md) | **Real fal.ai `image.inpaint` adapter built** (`packages/providers/adapters/fal-image.ts`). fal is slug `'fal'`, FIRST in the visual fallback order but reference-only until the owner provisioned a key (2026-07-23). First ASYNC adapter (queue submit→poll→result, transport verified vs fal docs) and first URL-in/URL-out adapter — first real consumer of the Eli-Q2a `ctx.signInputUrl` seam + URL-output → `normalizeToAssetKeys` path. Registered in the two harness scripts; wire-connection + validate-inpaint gained a `fal` branch. **Post-build audit:** official fal API page confirms the default `fal-ai/flux-general/inpainting` endpoint/schema; corrected missing `negative_prompt` support and missing explicit output `image_size`. Pricing remains unverified (no catalog rate invented → honest chain-head fallback). **NO live fal call** (owner gate: no synthetic fixtures; and fal needs an S3-backed store for publicly-reachable signed input URLs — local paths can't reach fal). **Scope:** §9.1 still-image lane and a future §9.2 content-replacement dependency; this is not `video.inpaint` or the complete hosted §9.2/§9.5 path. Plumbing validated by mocks only — repair quality still OPEN. | RESOLVED (adapter build) · live-validation OPEN | VHE-2 §7 · §9.1 · VHE-3 | `CC-OPUS-01` + audit `CODEX-SOL-01` | 2026-07-23 |

| [0027](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0027_s3-presign-store-and-section9-2-keyframe-orchestration_CC-OPUS-01.md) | **S3-backed AssetStore (SigV4 presign) + §9.2 keyframe content-replacement orchestration (no-spend).** (1) Built `packages/storage/s3-store.ts`: dependency-free AWS SigV4 query-presign + vetted client PUT. Post-build audit added an exact AWS S3 query-presign known-answer, expiry bounds, and concrete-region enforcement. (2) Built `packages/repair/keyframe-repair.ts`: routes global keyframes through hardened §9.1 with one fixed seed. Audit restored the blueprint-required **shared reference** end-to-end and completed fal `reference_image_url` support. NO RIFE interpolation, NO `video.inpaint`/removal lane. **153/153 green (17 files); preflight 13/4/1 unchanged.** No live call, synthetic fixtures, key exposure, or spend. Baseline `96fc986`; checkpoint audit by Codex Sol. | RESOLVED (both builds green) · live/quality OPEN | VHE-2 §7 · §9.1 · §9.2 · §9.5 | `CC-OPUS-01` + audit `CODEX-SOL-01` | 2026-07-23 |

| [0028](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0028_video-removal-lane-provider-comparison_CC-OPUS-01.md) | **Video-removal lane provider comparison (decision memo) + owner ruling.** Compared hosted `video.inpaint` (fal Wan VACE) vs hosted ProPainter-equivalent removal (fal VOID) vs Replicate ProPainter across cost/quality/mask/limits/async/reliability/router-fit. **Owner ruled (§7a, same day):** build **fal VOID** first for removal; Wan VACE reserved for content-replacement, NOT default removal; Replicate ProPainter NOT integrated now; register VOID under the existing `video.inpaint` capability (no new enum); seedless-provider output caching/retry-dedup + full lineage (model/version, source hash, mask hash, params, asset) required; a **zero-spend-only** limits probe is authorized; the mask-video encoder needs its own **separate additive spec logged first** before any encoder/adapter/test code. **Nothing implemented yet** — next session's first task is that spec. | **RULED — build authorized, spec-first, nothing built** | VHE-2 §9.2 · §7 | `CC-OPUS-01` | 2026-07-23 |

| [0029](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0029_removal-lane-mask-video-spec_CC-OPUS-01.md) | **Additive removal-lane specification (mask-video encoder + VOID adapter contract) — SPEC ONLY, no code.** The gated first-build task required by `0028` §7a ruling #5, written BEFORE any encoder/adapter/test code. Specifies: (1) per-frame §5 PNG masks → deterministic **lossless** `mask.mp4` via a NEW §6-wrapper recipe (`encodeMidArgs`'s `-crf 18`/`yuv420p` is lossy → unsafe for a binary mask, reconstruction argued); (2) exact source fps(num/den)/dims/frame-count/frame-alignment contract over the padded window; (3) fal VOID mask polarity + pixel format as the #1 zero-spend-probe unknown; (4) §6-wrapper-only implementation plan; (5) golden-test plan (determinism, alignment, polarity, round-trip IoU); plus the ruling-#2 capability mapping (`video.inpaint` + `extra.operation="remove"`) and the ruling-#3 caching/retry-dedup/lineage record shape. **Nothing built or probed** — this is the review gate before implementation. | **SPEC — SUPERSEDED by `0031` for implementation** (filed body + 14-item correction preserved as history; read `0031` to build) | VHE-2 §9.2 · §7 · §6 · §5 · §8 | `CC-OPUS-01` | 2026-07-24 |

| [0030](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0030_backup-zip-flatten-and-untracked-omission-defect_CC-OPUS-01.md) | **Backup/review zip defect found + fixed: `Compress-Archive -LiteralPath <array>` silently flattens all directory structure** (confirmed on v06 full backup + the first Progress-update 01 build) — a real collision (`_LOGS/README.md` vs `fixtures/_TEMP-provider-validation/README.md`) silently overwrote one file with the other. Same file-list method also **omitted every untracked file except the one hardcoded Higgsfield exception** — `0028`, `0029`, the 4 same-session handoffs, and `_IN-PROGRESS_CC-OPUS-01.md` were all missing from both zips. **Fixed:** rebuilt both from a staged directory copy (`Compress-Archive -Path "<dir>\*"`, not `-LiteralPath` on a flat file array), which preserves real relative paths; untracked file list now includes the pending log/handoff files explicitly, not just the one docx. Defective `v06` renamed `..._DEFECTIVE-FLATTENED-DO-NOT-USE.zip` (kept, not deleted) and superseded by verified `VHE-BACKUP-FULL_v07_2026-07-24.zip`; `VHE-Progress-update 01_2026-07-24.zip` rebuilt in place (not yet sent to any reviewer) plus a new `EXCLUDED-BINARIES-MANIFEST.md` (path/size/sha256 for the 8 stripped binaries) added inside the review zip. Both verified post-fix: entry counts, both README.md's coexist, all owner-named required files present, no `.env`. | **RESOLVED** | N/A (packaging/tooling, not a blueprint section) | `CC-OPUS-01` | 2026-07-24 |

| [0031](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0031_removal-lane-revised-spec_CC-OPUS-01.md) | **REVISED removal-lane specification — successor to 0029, folds all 14 Eli-review items + the owner's 2 design decisions.** Supersedes `0029` for implementation (0029's filed body stays as history; where they conflict, 0031 governs). Resolves the two open questions: **(D1)** ONE registered `fal` adapter keyed by slug with internal capability/model dispatch — NOT a slug+capability registry (provider-level state stays unified); **(D2)** a mask missing inside `[a..b]` is a HARD-FAIL before submission for v1, with ONE targeted re-track allowed (nearest valid tracked mask as new seed), else fail `MASK_TRACK_GAP` with the missing frame indices and no provider call. Folds: typed `sourceVideoKey`/mask-video request fields + rational `fpsNum/fpsDen` (items 1–2), validated `operation` request field + manifest `operations[]` + `manifestSatisfies` filter (item 3), worker-materialized provider-ready mask with TWO masks maintained — hard provider decision mask vs soft local blend mask (items 5, 10), non-binding codec deferred to probe with decoded-frame identity mandatory (item 6), provider-minimum pad-both-then-trim (item 7), pre-routing processing resolution + composite-back-at-native (item 8), MIME threading + bounded download (item 13), durable job claim + persist-jobId-after-submit + no fall-through once accepted + idempotency-key probe (item 11), durable cache/lineage with apply-time `inpainted_from` edge (item 12), §9.5 chunked-removal window contract (item 14). **SPEC ONLY — no code, no probe, no spend; STOP for re-review before any build.** | **SUPERSEDED by `0032` for implementation. Frozen as history — body + exactly ONE appended round-2 correction (round 1's 14 items were folded into the body, never a second appended block); no further correction will be appended.** | VHE-2 §9.2 · §9.5 · §7 · §6 · §5 · §8 | `CC-OPUS-01` + correction `CC-SONNET-01` | 2026-07-24 |

| [0032](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0032_removal-lane-consolidated-implementation-spec_CC-SONNET-01.md) | **Consolidated removal-lane implementation specification — clean successor to 0031 (round-4 rewrite, not an appended correction), per Eli's round-3 instruction.** Round 3 verdict on `Progress-update 04`: package healthy, spec-only, all 9 round-2 items genuinely present in `0031`, but still not build-authorized — several corrections described direction without fully resolving the implementation contract, and the filed body/test/build sections stayed contradictory. `0032` fully resolves ten named items: (1) a **discriminated request schema** making `operation` structurally required for every `video.inpaint` request, with `remove`-specific fields required+validated, not merely conditional prose; (2) a durable **`ProviderOperationRef`** type threaded through an actual `poll()`/`cancel()` signature change on `ProviderAdapter` (storing handler identity alone was insufficient — the plumbing itself now enforces correct dispatch); (3) **two-stage routing scoped to the removal lane only** (generic routing's dimension check is unchanged), a precise 3-way fall-through rule (unaccepted → may fall through; unknown/accepted → must not), and a **pre-job route/cost plan** feeding the §4 budget gate before any claim; (4) **one canonical ambiguous-submit state, `submission_unknown`** (retiring the second candidate name), wired into a full lifecycle enum as non-retryable, with a required test; (5) a concrete **`provider_operations` table**, unique-scoped `(ownerId, providerConnectionId, cacheKey)`, `resultStorageKey` split from the Apply-time asset, **7-day default retention** (owner-overridable, no longer `[OPEN]`); (6) a **cache key built from stable source identity/range/processing-recipe + canonical decision-mask frame hash** — explicitly not container-byte hashes, which are recorded separately as `submittedSourceHash`/`submittedMaskHash` for audit only; (7) a determinism hierarchy that **replaces `0031` §12.3 outright** (decision-region identity mandatory; raw-pixel identity mandatory only if the accepted format is lossless; container-byte optional; stop-and-ask if metadata can't establish threshold behavior); (8) **exact padding spec** — pre-pad repeats the first true-window frame, post-pad repeats the last, mask pad stays black, frame counts computed from rational fps only, **plus new audio handling** (video-only submission to the provider, original audio preserved and remapped at final splice — `0031` never addressed audio); (9) **`chunked-repair.ts` refactored into a provider-agnostic window/resume/overlap core** with seed/keyframe fields excluded from the seedless VOID lane by construction (no seed parameter in the core's per-child signature at all, not a runtime skip-branch), plus a new **`awaiting_approval`** halt state when overlap SSIM fails its threshold (no silent blend, no automatic re-purchase); (10) a consolidated probe-first build order (§13) and a single authoritative golden-test list (§12) with all field names current (`resultStorageKey` throughout). **SPEC ONLY — no code, no probe, no spend; STOP for re-review (round 4) before any build.** | **SPEC — BASE spec; round-4 review returned 9 final corrections → binding correction `0033` (0033 governs on conflict); STILL NOT build-authorized; NOTHING built/probed** | VHE-2 §9.2 · §9.5 · §7 · §6 · §5 · §8 · §4 | `CC-SONNET-01` | 2026-07-24 |

| [0033](ISSUE-RESOLUTION-LOG/VHE-ISSUE-LOG-0033_removal-lane-binding-correction_CC-OPUS-01.md) | **Binding correction to `0032` (Eli round-4 final corrections) — `0032` stays the base spec; where they conflict, `0033` governs.** All 9 items grounded against real code at `52e3277`: (1) **preserve the complete 9-member request family** — `0032`'s illustrated `GenRequest` union would have dropped `image.t2i`/`i2i`/`upscale`/`inpaint.identity_ref`/`video.t2v`/`i2v`/`extend`; define the `video.inpaint` sub-union **additively**; and make dispatch **provider-neutral** (`dispatchKey: string`, not a fal-only `'fal:image'|'fal:void'` union on the global `ProviderAdapter`); (2) **separate provider-operation state from job state** — `awaiting_approval` stays on the repair child job (`jobs.status` already has it), provider-op machine is `claimed→submitting→in_flight→succeeded|failed` with `submitting→submission_unknown` (retire `0032`'s `submitted`), add a **non-retryable `PROVIDER_SUBMISSION_UNKNOWN`** code so the BullMQ retry path never resubmits; (3) **concrete `provider_operations` schema in TEXT ULIDs** (not UUID) with real FKs to `jobs(id)`/`provider_connections(id)`, provider-output vs validated-result keys+MIME, `succeeded_at`-**anchored** 7-day retention (not `created_at`), state-driven cache behavior; (4) **two output artifacts** — raw `provider_output_key` (audit only) vs composited `result_key`; **only `result_key`** is previewed/applied/promoted to `media_assets`/`inpainted_from`; (5) **complete cache key** on source asset id + stored SHA-256 + true range + pad counts + dims + rational fps + operation + model/endpoint/revision + mask polarity/threshold + encoder/compositor recipe versions + canonical decision-mask frame hash (container hashes audit-only); (6) **budget + fall-through safety** — reserve the conservative max across the fallback chain (or recheck per candidate), and treat timeout/dropped-connection/5xx as **ambiguous, never proof of non-acceptance** (fall-through only after a proven pre-acceptance rejection); (7) **deterministic padding** — v1 places all provider-minimum padding **after** the true window (repeat last frame; black mask pad), preserving the start index; (8) **finish the overlap-approval contract** — define the minimum decision endpoint (accept / rerun-with-cost-confirmation-and-new-attempt-key / cancel; rerun passes the budget gate, never automatic; SSIM on composited regions, not raw provider frames); (9) **spend-safe build order** — durable table/state-machine/cache/reconciliation/retry protections **before** any live VOID submit path (VOID handler feature-gated until then) + 7 added tests. **SPEC ONLY — no code, no probe, no spend. Room closing; awaiting review in the new rooms.** **Round-5 correction appended 2026-07-24 (`CC-OPUS-01`, new room):** Eli's review of `Progress-update 06` approved the package **only for the zero-spend probe** (still probe-gated on Ashley's separate key/network go — not run), removal-lane build still NOT authorized; 8 final corrections appended (final discriminated `video.inpaint` remove/replace union; manifest `operations[]` + rational-fps bounds; full `submission_unknown` reconciliation with zero resubmit; atomic claim + `provider_operation_attempts` history table; structured `SubmissionResult` — only `preaccept_rejected` may fall through; **one** budget design chosen = conservative-max reservation across the eligible chain; idempotent accept/rerun/cancel; exact 6-step spend-safe build order) + verified ZIP record (SHA `32d5b…fe51`, **195,431 bytes** — reviewer's 462,775 was a slip). **Round-6 correction appended 2026-07-24 (`CC-OPUS-01`):** Eli's review of `Progress-update 07` passed the package audit but returned **6 removal-lane blockers** (still probe-only; build rejected): (1) legacy request arm must use `Exclude<Capability,'video.inpaint'>` so `video.inpaint` w/o `operation` is unrepresentable; (2) pick ONE rational-fps manifest shape = `min/maxFpsNum/Den` integer bounds, no supported-list alternative; (3) exact repair-job transitions out of `submission_unknown` — park in a new additive `awaiting_reconciliation` (not terminal `failed`), reconcile→`running`+poll-only or →`failed`, never resubmit; (4) retry reuses the SAME `(provider_connection_id,cache_key)` operation row + appends an immutable attempt row (a new operation row would violate the unique index); (5) concrete `budget_reservations` table + atomic reserve/release/reconcile txn (SELECT…FOR UPDATE on `budgets`), stays `held` through the unknown case; (6) exact parent/child accept/rerun/cancel transitions, accept advances with the composited `result_key`. Plus self-corrections: my round-5 "462,775 = transcription slip" was WRONG (462,775 = 06's uncompressed content, 195,431 = compressed zip — both correct), and the fix for 07's stale in-zip build-metric placeholders. All grounded on real `jobs.status`/`budgets` schema. **Round-7 correction appended 2026-07-24 (`CC-OPUS-01`, Opus 5):** Eli's review of `Progress-update 09` **REJECTED the specification** with 6 remaining blockers + a required all-seven-status resume table; all 6 verified as **genuine defects in my own round-5/round-6 text** against real code at `cbc094e`: (1) `worker.ts:66-73` writes `status='succeeded'` **unconditionally** after the handler returns, so R6·3's "set `awaiting_reconciliation` and return" is clobbered microseconds later (job reports success with no result, reservation held forever) → discriminated `HandlerOutcome` (`completed`|`parked`) + **guarded** terminal writes (`AND status='running'`) + widened `publishState`; (2) `worker.ts:35-45`'s verbatim claim cannot take a freshly-`running` job (fresh heartbeat ⇒ 0 rows ⇒ silently dropped; `awaiting_reconciliation` not in the IN-list; `attempt+1` burns the retry ceiling) → additive resume claim that performs `awaiting_reconciliation → running` **itself**, no attempt increment, + a persisted `resumeMode:'poll_only'` marker in `input` so even the verbatim 120s stale-takeover path cannot re-enter the submit branch; (3) `chunked-repair.ts:123,126-131,174-182` — `ChildStatus` lacks `awaiting_reconciliation` and `firstNonSucceededWindow`'s `!== 'succeeded'` rule makes the parent **re-run and re-charge** children parked in `awaiting_approval`/`awaiting_reconciliation`/`canceled` → disposition classifier, `deps.run` **only** on `queued`/absent, + the **exact 7-status parent/child resume table**; (4) R6·5 released the reservation on `preaccept_rejected` — precisely when `walkChain` (`routing.ts:115-136`) advances to the next, possibly costlier, candidate, destroying R5·6's chain-max design and reopening its TOCTOU window → reservation lifetime = **the whole chain walk**, released only on chain-exhausted/cancel/confirmed-no-job, + a chain-snapshot invariant; (5) `budget_reservations` had no attempt reference (two `held` rows per operation indistinguishable ⇒ wrong release or orphaned hold) and reconcile **SET** `jobs.cost_cents` would erase prior attempts' spend from the live `SUM` (`create.ts:59-67`, `schema:104`) → `provider_operation_attempt_id` FK as the reservation grain + `jobs.cost_cents` as a **derived attempt rollup** (budget model unchanged) + parent `cost_cents=0` to stop chunked double-counting; (6) no DB uniqueness on `attempt_key` or reservations ⇒ the promised rerun dedup was app-level TOCTOU (two clicks = two paid submits) → unique indexes + claim-on-insert mirroring the project's proven `jobs.idempotency_key`/`ON CONFLICT DO NOTHING` pattern, plus a 6-step ordered spend-capable sequence and 8 new tests. Also corrects the stale `52e3277` baseline cited by rounds 5–6 (**real HEAD `cbc094e`**) and records that the Higgsfield addon docx decision is now **resolved** (tracked). **Round-8 correction appended 2026-07-24 (`CC-OPUS-01`, Opus 5):** Eli's review of `Progress-update 10` **passed the package audit** (155 entries / 970,159 bytes / SHA `165E9B74…` all independently confirmed) but **REJECTED the specification again** with 6 blockers; all 6 confirmed correct, and **four force explicit retractions of my own round-7 text**: (1) R7·1 guarded the `succeeded`/`failed` writes but **not the new `parked` write** — a concurrent cancel could be overwritten back into a non-terminal state holding a reservation → **one uniform** `WHERE id=$1 AND status='running' RETURNING id` guard for all three dispositions, publish only on exactly 1 row (also narrows R7·1's `failed` guard from `IN ('running','queued')`); (2) R7·2's `resumeMode:'poll_only'` in `jobs.input` is **sticky** — after reconciliation → `awaiting_approval` → rerun, the stale marker makes the worker **poll the old provider job instead of submitting the new attempt** → marker **RETRACTED**, execution mode + provider job id bound to the current attempt via `provider_operations.current_attempt_id` + `provider_operation_attempts.execution_mode`, so one pointer write clears all prior poll-only state; (3) **structural** — a chain-wide reservation cannot FK to a provider-specific attempt that doesn't exist until a candidate accepts (R7·5/R7·6's attempt-first ordering is unimplementable) → new provider-neutral **`routing_attempts`** table owns the reservation (`budget_reservations.routing_attempt_id` UNIQUE, `accepted_provider_operation_attempt_id` **nullable**, set after selection); each candidate still gets its own attempt FK'd back to the routing attempt; (4) R7·4/R6·5's **release-on-cancel** erases real spend — post-acceptance cancel/failure doesn't prove $0 → release **only** on proven pre-acceptance rejection or confirmed-no-job; accepted/ambiguous/failed-after-acceptance/canceled-after-acceptance all **reconcile**; unknown cost stays **`held`**; parent sibling-cancel applies the same per-sibling test; (5) R7·5's "budget model needs no migration" is **RETRACTED** — `periodSpendCents` (`create.ts:56-68`) filters on the **job's** `created_at`, so a rerun in a later month never counts against that month's cap → the **reservation ledger keyed on its own `period_start`** becomes authoritative (with a `NOT EXISTS` legacy fallback so no-reservation jobs count exactly once and dual jobs aren't double-counted), `jobs.cost_cents` demoted to a display rollup; (6) R7·6's "claim returns zero rows → STOP" **orphans a held reservation** → steps 1–4 become ONE **DB-only** transaction (submit deliberately outside the `FOR UPDATE`), + an explicit compensation path for every post-commit exit, + a **reaper** for the commit-then-crash window that applies the R8·4 test before releasing; idempotency moves up to `routing_attempts(routing_attempt_key)` UNIQUE with claim-on-insert. Plus `PACKAGE-PROVENANCE.md` in future packages (response to Eli's note that HEAD can't be authenticated from a ZIP without `.git` — self-asserted, labeled as such) and 8 new tests. **Admin rulings recorded:** keep signing `CC-OPUS-01` with "Claude Opus 5" as the current model version in the existing registry row — **no `CC-OPUS-02` split** (round-7's open question CLOSED); one docs-only commit after the logs are updated; cut `Progress-update 11` and stop. **If round 8 clears, the next gate is the separately authorized zero-spend fal metadata probe — NOT implementation.** **Round-9 correction appended 2026-07-24 (`CC-OPUS-01`, Opus 5):** Eli's review of `Progress-update 11` **passed the package audit** (157 entries / 986,142 bytes / SHA `204771A8…`; **`0033` verified append-only with Update 10's `0033` an exact prefix**; **`0032` byte-for-byte unchanged**; no source changed 10→11) but **REJECTED the specification again** with 5 blockers — all correct, **two of them self-contradictions I introduced in round 8**: (1) **R8·3 and R8·6 literally contradict each other** on the same event ("operation claim affects zero rows" = candidate skip vs. full rollback), and both are wrong because a claim miss proves nothing about the candidate → the miss must **branch on the existing operation state** (`succeeded`→reuse `result_key` zero-spend / `submitting`,`in_flight`→attach+wait / `submission_unknown`→park / `failed`→permitted-retry rule), **never auto-skip to a second paid operation**; transaction model restructured into **TX-A** (routing claim + chain reservation, once) + **TX-B** (per candidate: operation upsert, attempt insert, claim) with rollback scoped to **that candidate's attempt only**; plus the previously-undefined reservation handling for zero-spend cache reuse (**release**) and attachment to a running operation (**release** — the owning walk's reservation covers it; invariant: exactly one held reservation per billable operation); (2) **R8·6 reintroduced the "not proven accepted ⇒ proven not accepted" fallacy** that rounds 5–6 had already outlawed, and its reaper guarded `accepted`/`submission_unknown` but **not `submitting`** — the exact crash-after-transmit state → durable **`prepared`→`submitting` boundary committed BEFORE the first byte**, so only `prepared` rows provably never transmitted may be abandoned+released while crashed `submitting` rows resolve to `submission_unknown` and stay `held`; compensation now follows the same structured `SubmissionResult` rules as the normal path; (3) `provider_operations.current_attempt_id` sits on the **shared** operation row and cannot say which routing attempt authorizes **this job** → new **`job_execution_bindings`** table (job → routing attempt → provider attempt) plus **six composite FKs** making cross-operation/cross-routing pointers structurally impossible, and a rerun that atomically repoints the binding before the job is executable; (4) R8·5's `NOT EXISTS` fallback **excluded a job's entire pre-ledger `cost_cents`** once any reservation existed ($5 pre-ledger + $3 reconciled rerun counted $3, not $8) → **backfill historical spend as frozen `origin='backfill'` reconciled ledger rows** and **delete the fallback term entirely**, with nullable `routing_attempt_id` + partial unique index + a crossover guard; (5) R7·3 left the chunk parent `running` **with no heartbeat** after the handler returns (`worker.ts:83-85` clears the interval), so it goes stale at 120s and is repeatedly taken over with inflating `attempt` → additive **`awaiting_children`** parked state, guarded no-attempt-increment resume, child-terminal-transition wake-up enqueued **in the same transaction** as the child's state write, plus a sweeper; governing invariant: **a parent may never remain `running` without a live owner and heartbeat**. Plus R9·0 correcting my own inaccurate "`.env.example` is all-empty" wording (it is credential-free but carries the non-secret defaults `S3_REGION=auto` and `VHE_REPAIR_MEMORY_CEILING_BYTES=4294967296`) and 8 new tests. **Gate direction: the next gate is round-nine SPEC REVIEW, not the probe — the probe becomes next only after the complete specification is approved.** **Round-10 correction appended 2026-07-26 (`CC-OPUS-01`, Opus 5):** Eli's review of `Progress-update 12` **passed the package audit** but **REJECTED the specification again** with 4 blockers + 1 documentation correction; all confirmed against real code, and **blockers 1–2 are contradictions inside my own round-9 text**: (1) R9·1's claim-miss table let an attaching worker **poll `provider_job_id` in `submitting`**, but R9·2 captures that id only at `accepted`/`in_flight` — **there is nothing to poll** → table split so **only `in_flight` polls a durable id**, a **live** `submitting` waits on the owner's lease, a **stale** one becomes `submission_unknown` and parks (reservation stays `held`); also adds the **`prepared`** rows R9·1 never had (live→wait, stale→reaper abandons+releases and the waiter re-evaluates) — my own find, not Eli's; (2) R9·1 permits Job B to attach to Job A's operation while R9·3's composite FKs require every binding to be **same-job**, so a follower **cannot legally record what it attached to** → new **`provider_operation_followers`** table at the provider-operation level (PK `(provider_operation_id, follower_job_id)`, composite FK binding the follower's own walk to itself), terminal/reconciliation wake-up per follower, and `job_execution_bindings` FKs **left unweakened**; plus the consequence R9·1 created and never followed through — a follower woken by a **`failed`** operation holds **no** reservation (R9·1 released it at attach) and **must re-acquire one before any paid attempt**, failing on the cap rather than proceeding unfunded; (3) R9·4 deleted the budget fallback and left the cap reading **only** ledger rows, which only the routing path writes — verified worse than "future jobs": `worker.ts:68-72` is the **universal** §4.2 terminal-success write billing every job type with no routing attempt, `create.ts:56-68` is today's only spend reader, and `schema:104` states the blueprint's own `SUM(jobs.cost_cents)` model, so the **already-built** §9.1/§9.2 content-replacement lane would have vanished from the cap → third ledger origin **`origin='direct'`** (reconciled, `routing_attempt_id IS NULL`, `UNIQUE (job_id) WHERE origin='direct'`) written **in the same Postgres transaction** as the terminal `jobs` write; new invariant: **every cent in `jobs.cost_cents` appears exactly once as a reconciled ledger row**; the §4.2 **verbatim-boundary edit site is flagged as an OPEN DECISION for the owner**, not chosen, and the pre-existing non-routed cap-overshoot gap is named but deliberately not fixed; (4) R9·5 promised the child's terminal write and the parent wake-up **enqueue in one transaction**, but state is Postgres and the queue is **BullMQ over ioredis** (`queue/connection.ts:40-52`, `queue/queues.ts:71-72`) — **that atomicity does not exist** → **RETRACTED** and replaced by a transactional **`job_wakeup_outbox`** row inserted in the same Postgres transaction (real atomicity) + a dispatcher + the retained sweeper; effectively-once resume comes from outbox ∧ BullMQ `{jobId}` dedupe (`queues.ts:79`, `runtime.test.ts:173-179`) ∧ R9·5's guarded single-winner claim, stated as a chain; and the durable location Eli asked for — new **`job_parent_blocks`** (PK `parent_job_id`, composite FK `(blocking_child_job_id, parent_job_id) → jobs (id, parent_job_id)`) so a block on a non-child is structurally impossible. Plus **R10·0** correcting my own summary's "six composite FKs" — **R9·3 defines five composite FKs + one single-column FK**; and 10 new tests. **Acceptance returned: (b) TX-A/TX-B and (c) `prepared→submitting` ACCEPTED; (d) accepted for same-job execution only; (e) backfill accepted but incomplete without the direct path; (f) `awaiting_children` accepted but requires the outbox; (a) not accepted until 1–2 are fixed.** **Gate: another SPEC REVIEW, still not the probe.** **Round-11 correction appended 2026-07-27 (`CC-OPUS-01`, Opus 5):** Eli's review of `Progress-update 13` **passed the package audit** but **REJECTED the specification again** with **7 blockers** — all confirmed with file:line before writing, and **five are defects introduced by round 10's own correction**, three of them contradictions between a round-10 rule and a round-10 **test**: (1) R10·2 defined the follower *relationship* state but never the follower **job's** `jobs.status` — `worker.ts:68-72` would mark a parked follower **`succeeded`**, and staying `running` reproduces R9·5's 120s takeover loop with inflating `attempt` (`worker.ts:37`,`:39-40`) → additive **`awaiting_provider_operation`** state, R8·1 guarded park, separate guarded resume with **no `attempt` increment**, one-transaction structural link to the follower row, and cancel that resolves the follower without touching the owning operation; plus my own generalization that **every guarded write names the exact state it replaces** (a blanket `status='running'` guard silently no-ops on parked jobs); (2) R10·2's wake-up set (`succeeded`/`failed`/`submission_unknown`) **omits `in_flight`, which round-10 test 1 requires**, and wakes only `waiting` followers so a `submission_unknown`-`parked` follower is **never woken again** (durable dead end) → **followers never poll** (owner/reconciler own all provider polling; R10·1's `in_flight` "attach and poll" cell retracted), the `parked` follower state is **retracted** in favor of re-parking as `waiting`, and the wake set is stated exactly: `succeeded` · `failed` · `submission_unknown` · **`in_flight`** · every reconciliation transition out of `submission_unknown`; (3) R10·1 (`failed` ⇒ R6·4 same-operation retry) and R10·2 (`failed` ⇒ advance to the next candidate) prescribe **different actions for the same input**, and generic `failed` does not prove pre-acceptance rejection — R5·5 makes **`preaccept_rejected`** the only variant permitted to fall through and R8·4 establishes accepted-then-failed may have **billed** → branch decided by the **structured submission outcome**, never the label; (4) **BullMQ's `{jobId}` dedupe does the opposite of what R10·4 claimed** — `queues.ts:85` uses the DB job id as the custom job id, **`removeOnComplete` appears nowhere in `packages/`** (grep: zero hits) and `bullmq` 5.80.1 retains completed jobs, so re-adding `target_job_id` is **ignored and the resume is never enqueued** → two identities: delivery id **`wake:{outbox_id}`** + payload `{targetJobId, wakeKind, outboxId}`; also records that `runtime.test.ts:173-179` proves dedupe only for a **waiting** job, not a completed one, and that this was **not** measured live this session; (5) the transport cannot reach any resume claim — `runtime.ts:49` types the payload as `{jobId}` only, `:62` always calls `claimForExecution`, which admits only `queued`/stale `running`, so **every rounds-9/10 resume is silently dropped at `:66`** → durable **`wake_kind`** (`execute`/`resume_children`/`resume_provider_follower`/`resume_reconciliation`) on the outbox row and payload, dispatched explicitly, **never inferred from mutable status**; (6) R10·4 says the sweeper only **inserts outbox rows** yet claims it covers "a dispatcher outage entirely," while round-10 **test 10** asserts the sweeper alone resumes a parent **with the dispatcher stopped** — impossible → the sweeper repairs missing **intent**, delivery availability comes from **redundant dispatchers** (wake-ups delayed, not lost); **round-10 test 10 RETRACTED**; plus my own find that the sweeper's re-insert needs **`UNIQUE (target_job_id, wake_kind) WHERE dispatched_at IS NULL`** — a blanket key would block the very re-wake the sweeper exists to provide; (7) `UNIQUE (job_id) WHERE origin='direct'` written only beside terminal success has the **wrong billing grain** — the worker runs up to 3 attempts (`worker.ts:76`, `queues.ts:59-62`) and R8·4 requires reconciling accepted-then-failed/canceled charges → grain becomes **one billable execution attempt** (`UNIQUE (job_id, execution_attempt) WHERE origin='direct'`, or a provider charge id), `jobs.cost_cents` a derived rollup, and **`reserved_cents = 0`** stated explicitly for **`backfill` as well as `direct`** (R6·5 declares it `NOT NULL`; neither R9·4's nor R10·3's row supplied a value — the `backfill` half is my find). Plus **two consequences traced into the §4.2 body that Eli did not name**: the failure path has **no cost channel at all** (`worker.ts:77-81` writes no `cost_cents`; `ApiError` at `errors.ts:28-37` carries only `code`/`httpStatus`), so a charged failure is unreportable — filed as an open spec question, not decided; and a **derived `cost_cents` collides with the verbatim `cost_cents=$3` write** (`worker.ts:69-71`), which strengthens but does not decide the §4.2 OPEN DECISION. **Eli recommends option (iii)** (documented VHE-2 §4.2 amendment; short Postgres transaction, never held across a provider call) — **recorded, not enacted; Ashley decides.** Admin: **keep the six `.docx` blueprints in review packages** (closes handoff 40's question), **no `v09`**, and **no `Progress-update 14` until round 11 is filed and committed**. 10 new tests (round-10 test 10 replaced), and a new standing pre-filing step: **read the round's own tests back against the round's own rules** — the check that would have caught three of these seven.  **Round-12 correction appended 2026-07-27 (`CC-OPUS-01`, Opus 5):** Eli reviewed the actual `Progress-update 14` ZIP — **package audit PASS** with every metric independently reproduced (164 entries, 1,049,701 compressed, SHA `5F14F62A…C20EC8E`, **Update 13 `0033` an exact prefix of 14 s**, `0032` byte-for-byte unchanged, no source changed 13→14) — but **REJECTED the specification** with **8 blockers**, **seven of them defects in round 11 s own correction**: (1) **`wake:{outbox_id}` is not a valid BullMQ custom job id** — verified against the *actually installed* source at `node_modules/bullmq/dist/cjs/classes/job.js:1049-1051`, which **throws `Custom Id cannot contain :`** for any colon id not splitting into exactly 3 parts, so every round-11 wake delivery would have failed at the queue boundary → **`wake-{outbox_id}`** (+ `-{dispatch_generation}`), plus my own binding note that the 3-part colon exemption must NOT be used since the adjacent source comments mark it a legacy carve-out slated for removal in BullMQ s next breaking change; (2) followers still woken for **non-actionable** `in_flight`/`submission_unknown`, which also opens a **lost-wake race** (follower transiently `running` ⇒ the terminal wake s resume claim matches zero rows ⇒ falls back to the sweeper) → `in_flight` wake **RETRACTED**, **round-10 test 1 retracted in full** (round 11 had reshaped its *rule* to satisfy that stale *test* — the exact failure its own new discipline was meant to prevent), actionable-only wake set stated, and **operation-level reconciler adoption** defined so an accepted operation orphaned by its owner s death never strands its followers; (3) **`dispatched_at` does not prove consumption** — the R11·6 index stops protecting the instant the dispatcher stamps it → durable delivery lifecycle (`dispatch_generation`, `dispatch_lease_expires_at`, **`consumed_at` written in the SAME Postgres transaction as the winning resume claim**), sweeper reissues **only** after an unconsumed lease expires, and the index predicate corrected to **`WHERE consumed_at IS NULL`**; (4) **a crash AFTER the resume claim strands the job at `running`** — the retried wake cannot claim it (predicate is `awaiting_provider_operation` only) and `claimForExecution` (`worker.ts:35-45`) would take it over, **increment `attempt` (`worker.ts:37`) and drop a follower into the initial-submit branch** → durable continuation marker + per-mode stale continuation takeover that preserves execution mode, never increments the paid attempt, and never falls into initial submit; (5) **R11·3 ↔ R8·6 are unimplementable together** — R11·3 told a follower to acquire a fresh reservation while R8·6 (`0033:1324-1326`) mandates a **full** `UNIQUE (routing_attempt_id)` in which *a released row must still block a second reservation* (`0033:980-985` gives the double-charge reasoning) → the continuation **mints a NEW routing attempt** (new deterministic key, remaining-candidates snapshot, one new reservation, atomic binding repoint, old attachment resolved) and **never reopens the released walk**; (6) follower **PK `(provider_operation_id, follower_job_id)` forbids a job from ever following the same durable operation twice** (a legal rerun has no legal move) → own **`id` PK** + partial unique **`WHERE state='waiting'`** (one *active* attachment), parked job links to the exact attachment id, resolved history immutable, R10·2 composite PK **RETRACTED**, composite FK **unweakened**; (7) the direct-ledger key was still **an alternative rather than a schema** → single grain **`UNIQUE (job_id, execution_attempt) WHERE origin='direct'`** + optional `provider_charge_id` with its own partial unique guard + explicit per-origin null/non-null CHECKs + `reserved_cents=0` for `backfill` and `direct`; (8) the queue payload was **still two contracts** (`execute` as a `wake_kind` AND legacy `{jobId}`) → Eli s exact discriminated **`QueueDelivery`** union adopted verbatim, `execute` removed from the `wake_kind` set, unknown shapes fail loudly, and the **queue name derived from authoritative `jobs.type`** (never an unbound copied `target_job_type`). **OWNER DECISION RECORDED — Ashley AUTHORIZES option (iii): a documented amendment to VHE-2 §4.2**, with a **typed execution failure/result contract** (frozen claim-time `execution_attempt`, provider/operation refs, submission disposition, charge state, known cost, retryability + machine code); known billed failures hit the ledger before retry/terminate; accepted-or-ambiguous-with-unknown-cost enters reconciliation and never auto-retries; **`jobs.cost_cents` recomputed from the reconciled ledger sum and the `cost_cents=` assignment amended so it never overwrites the rollup**; terminal state + ledger + rollup in ONE short Postgres transaction; **no DB transaction ever held open across a network call**. This closes the round-10 OPEN DECISION and round 11 s missing-cost-channel finding. **The blueprint `.docx` was deliberately NOT edited** — authorization recorded, the edit deferred to its own task (needs Ashley s direct go, a companion `_BLUEPRINTS-TEXT/_regenerate.py` run, and its own entry `0034`); the **§4.1 payload discriminant change is flagged as needing the same authorization** rather than assumed covered. 10 round-12 tests. **Discipline widened: read new rules against the ENTIRE live test list, all prior rounds included, and retract by number any test a new rule invalidates** — a surviving old test is the likeliest hiding place for a silent contradiction. **Round-13 correction appended 2026-07-27 (`CC-OPUS-01`, Opus 5):** Eli reviewed the actual `Progress-update 15` ZIP — **package audit PASS** with the strongest verification yet (167 entries, 1,078,864 bytes, SHA `AD953780…CED971`, **Update 14's `0033` an exact byte-prefix of 15's**, `0033` grown **2,435 → 2,916 lines = exactly 481 appended, zero prior-line changes**, `0032` and **all blueprint `.docx` unchanged**, no source changed, stale-provenance mistake confirmed corrected) — but **REJECTED the specification** with **7 blockers**. **The failure mode changed this round:** not contradictions against reality (rounds 11–12) but **incompleteness that reads as completeness** — an undefined term, a transaction with one branch, an unscoped constraint, a type that cannot hold its values, and **twice an "or" where an implementer needs a decision**: (1) the operation-level reconciler adopts only on a **terminal** owner (`0033:2553-2555`) while round-12 **test 3** requires **canceled OR stale** (`0033:2864-2865`) — a stale owner is not terminal — and no reconciler identity, lease, CAS, or delivery target was ever defined (an outbox row needs a `target_job_id`; after a terminal owner there is no job to target) → durable `reconciler_owner_id`/lease/`reconciler_generation` columns, **two explicit adoption predicates** (terminal owner; or `running` with `heartbeat_at < now() - 120s`, **reusing `worker.ts:39-40`'s existing staleness definition** rather than inventing a second), single-winner CAS adoption, generation-guarded lease renewal, and **R12·2's "adoption is an outbox-eligible event" RETRACTED** — reconciler polling is a durable DB scan, not a queue delivery; (2) **"continuation generation" occurs EXACTLY ONCE in 2,916 lines** (`0033:2666`) and is defined nowhere — the only defined generation is `dispatch_generation`, which increments per redelivery, so an implementer reaching for it would make a redelivery mint a **different** routing key and defeat the collapse guarantee → separate durable **`jobs.continuation_generation`**, incremented once per logical continuation, with the binding rule that **only it may enter a durable business key**; (3) R12·5's five-step transaction has **no failure branch** (`0033:2678-2680`) and a naive rollback would undo `consumed_at`, stranding a consumed wake on a still-`waiting` follower → **two commit shapes, never a rollback to nothing**: funded, or refused (no binding installed, no unfunded attempt executable, attachment resolved, job `failed`/`BUDGET_EXCEEDED`, marker cleared, **`consumed_at` written**) — all atomic; (4) the direct-ledger grain assumed one billable operation per execution attempt, but **`walkChain` (`routing.ts:115-136`) loops the whole chain inside one call** → **option 1 chosen** (`UNIQUE (job_id, execution_attempt, provider_attempt_no) WHERE origin='direct'`) **with the reason stated**: option 2 would change already-shipped §7 fall-through behavior, which is scope growth into built code — *option 1 records reality, option 2 changes it*; (5) `provider_charge_id`'s unique guard was **unscoped** (`0033:2739`), assuming a global charge-id namespace across all providers → **`UNIQUE (provider_connection_id, provider_charge_id) WHERE ... NOT NULL`**; (6) **`BIGSERIAL` cannot round-trip a JS `number`** — `job_wakeup_outbox.id BIGSERIAL` (`0033:1921`) vs `outboxId: number` (`0033:2774`), above 2^53 precision is lost and node-postgres returns `int8` as a string anyway → **`outboxId: string`**, regex-validated, never numerically coerced (`dispatchGeneration` stays `number`, stated so the fix isn't over-applied); plus **my own find**: `job.js:1045` also throws **`Custom Id cannot be integers`**, so the **`wake-` prefix is load-bearing**, not decoration; (7) the queue binding was **still two alternative schemas** (`0033:2788-2790`) — **the exact defect R12·7 had corrected a few paragraphs earlier in the same append** → `target_job_type` **REMOVED** from the outbox, dispatcher loads authoritative `jobs.type` in its own claim, composite-FK alternative **RETRACTED**. **The widened round-12 test-list discipline was actually EXECUTED this round** and reported: one test amended (r12 test 8, for the new `provider_attempt_no` dimension), one **rule** corrected to match its test (r12 test 3), **zero tests retracted**. 8 round-13 tests. **Blueprint sequencing — Eli's ruling adopted: `0034` must NOT be executed yet**; amending §4.1/§4.2 now would freeze incomplete contracts into both the mirror and the binary blueprint. After round 13 clears, Ashley authorizes **one scoped `0034`** covering §4.1's `kind:"execute"` payload AND §4.2's typed contract together, with `_regenerate.py` rerun in that same session. **New standing pre-filing check: grep the round's own new text for an "or" at a decision point, and confirm every new identifier is defined somewhere — a term used exactly once is a placeholder, not a specification.** **Round-14 correction appended 2026-07-31 (`GPT-5-01`, Eli Soren / ChatGPT GitHub lane): Update-16 GitHub migration PASS; specification REJECTED with 6 blockers — unified attempt-scoped poll lease, durable follower-continuation key, one atomic wake-to-next-state transaction, explicit `budget_refused` routing state/order, per-candidate `ProviderChargeEvent[]` transport for direct-ledger accounting, and 8 additive tests. No source/blueprint/probe/key/network/spend.** **Independent REVIEW-001 (Marcus Soren, 2026-08-01): REJECTED; R14·2 and R14·4 passed, five blockers confirmed. Round-15 correction appended by `GPT-5-01`: frozen claim-attempt fencing for every worker-owned job write, a separate durable execution outbox, one fresh §4 execution attempt per routed continuation, structured charge events preserved through total exhaustion, and 10 additive tests. No source/blueprint/probe/key/network/spend.** | **SPEC — binding correction to 0032 + rounds 5–15 appended; Round 14 independently REJECTED; Round 15 awaiting REVIEW-002; `0034` remains reserved/unexecuted; STILL NOT build-authorized; NOTHING built/probed** | VHE-2 §9.2 · §9.5 · §7 · §6 · §5 · §4 · §4.1/§4.2 · §2 | `CC-OPUS-01` + `GPT-5-01` | 2026-08-01 |

**Next available number: 0034 (reserved; do not claim while the specification gate remains open)**

---

## Open items requiring the project owner

- **`VHE-ISSUE-LOG-0009` / `0011` — deliver the 4 AI-content fixture files** (`bad_hand.png`,
  `garbled_text.png`, `melted_face_15s.mp4`, `bad_hand_6s.mp4`) as frozen files into `fixtures/`.
  Owner chose supply-as-files over granting a key. Blocks the Phase 0 exit gate.
- ~~**`VHE-ISSUE-LOG-0011` — provision hosted dev services and fill `.env`**~~ **DONE** (2026-07-20,
  `0011`): all 3 hosted dev services provisioned and connecting (S3/Redis PASS, PG via IPv4 pooler),
  `.env` on disk (gitignored). No longer blocks Phase 0 — the only remaining Phase-0 exit-gate FAILs
  are the 4 undelivered AI fixtures above. (Corrected 2026-07-21, `0021`: this item was stale.)
- `VHE-ISSUE-LOG-0003` — ~~Q2 (provider ranking)~~ **ANSWERED 2026-07-20 → `VHE-ISSUE-LOG-0018`; §7
  built in `0019`.** Still open: Q4 (desktop wrapper?), Q5 (team), Q6 (marketplace model). None block
  Phase 0. **Audio default order (Google→OpenAI→ElevenLabs) recorded in `0018` for the VHE-4 room.**
- **`VHE-ISSUE-LOG-0017` — confirm (or overrule) the two delegated rulings** that make §4's retry
  path actually run: (A) `attempts: 3` + `backoff: {type:'custom'}` added to §4.1's verbatim
  add-options; (B) release the row to `queued` on §4.2's graceful-handback rethrow. Both deviate from
  verbatim, both are one-line reversible, each is pinned by a test that turns red on revert. Blocks
  nothing — the code is built, tested live, and green.
- `VHE-ISSUE-LOG-0007` — where BullMQ workers + FFmpeg jobs run in production under the free-tier
  constraint. Blocks deployment work only, not local build. (Dev-side route decided in 0011.)

## End-of-project harvest

At project close this library gets reviewed for: recurring errors · corrections that consistently
worked · preventive tools, tests, wrappers and safeguards worth adding · reusable fixes for the
Soren Tools Library · patterns worth turning into platform skills · lessons for a revised blueprint.
Section 7 ("Prevention") of each entry is the harvest target. First confirmed harvest candidate:
`_BLUEPRINTS-TEXT/_regenerate.py` (stdlib-only .docx extractor, see 0004).
