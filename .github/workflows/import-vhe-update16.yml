name: Import VHE Update 16 operational baseline

on:
  push:
    paths:
      - 'migration-payload/READY'

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Reassemble and verify payload
        shell: bash
        run: |
          set -euo pipefail
          cat migration-payload/part-* > /tmp/vhe-update16.b64
          base64 --decode /tmp/vhe-update16.b64 > /tmp/vhe-update16.tar.xz
          echo "2c7e0a6ddd57c05b266fbd78653e7e92fe910f2f645ecde31026430eb8332979  /tmp/vhe-update16.tar.xz" | sha256sum -c -
          xz -t /tmp/vhe-update16.tar.xz

      - name: Import authoritative Update 16 operational tree
        shell: bash
        run: |
          set -euo pipefail
          tar -xJf /tmp/vhe-update16.tar.xz -C .
          rm -rf migration-payload
          rm -f .github/workflows/import-vhe-update16.yml

      - name: Commit imported baseline
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "Eli Soren"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No import changes detected."
            exit 0
          fi
          git commit -m "Import VHE Update 16 operational baseline — Eli Soren"
          git push origin HEAD:migration/update16
